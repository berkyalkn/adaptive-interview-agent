version: '3.8'

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ai-interview-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app  
    env_file:
      - ./backend/.env
